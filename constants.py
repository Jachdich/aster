from os.path import join
from datetime import datetime

config = "config.json"
styles = "styles.json"

PORT = 64452
HOST = "127.0.0.1"

indent_level = 4
current_logfile = str(datetime.now()).replace(":", "-")
